const Character = require('./Employee');

class Engineer {
    constructor(name) {
        super(name, id, email);
        this.github = github;
        this.role = "Engineer";
    }
        githubUrl() {
            const url = `github.com/${this.github}`
            return url;
        }
}

module.exports = Engineer;
